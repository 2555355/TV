pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 系统 WebView 是 Android SDK 自带,无需额外 Maven 仓库。
        // 已移除 Mozilla Maven 仓库(GeckoView 已弃用)。
    }
}
rootProject.name = "TvFoxBrowser"
include ':app'
