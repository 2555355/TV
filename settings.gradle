pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // GeckoView 发布在 mavenCentral(org.mozilla.geckoview:geckoview-arm64-v8a)
        // 无需额外镜像。原 Crosswalk 的 louisgeek 镜像已弃用。
    }
}
rootProject.name = "TvFoxBrowser"
include ':app'
