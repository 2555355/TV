<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- 顶栏:地址栏 + 工具按钮 -->
        <include
            android:id="@+id/top_bar"
            layout="@layout/view_top_bar" />

        <!-- 进度条 -->
        <ProgressBar
            android:id="@+id/page_progress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="3dp"
            android:max="100"
            android:progress="0"
            android:progressTint="@color/brand_secondary"
            android:visibility="gone" />

        <!-- WebView 容器(系统 WebView,替代 GeckoView) -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/bg_dark">

            <!-- TabManager 会把当前标签页的 WebView 动态 addView 到这里 -->
            <FrameLayout
                android:id="@+id/web_view_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

            <!-- 主页门户覆盖层,URL 为 about:home 时显示 -->
            <FrameLayout
                android:id="@+id/home_container"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone" />
        </FrameLayout>
    </LinearLayout>

    <!-- 全屏 Overlay(标签页 / 历史 / 书签 / 设置) -->
    <FrameLayout
        android:id="@+id/overlay_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 视频/网页全屏容器:WebChromeClient.onShowCustomView 会把视频 SurfaceView 加到这里,
         覆盖整个屏幕(包括顶栏)。B站/YouTube 等点全屏必需。 -->
    <FrameLayout
        android:id="@+id/fullscreen_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:visibility="gone" />

</FrameLayout>
