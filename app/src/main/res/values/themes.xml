<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- TV 暗色主题。不依赖 Theme.Leanback 主题(避免引入其 attr 依赖),
         使用 AppCompat 暗色主题 + 手写焦点样式。Leanback 仅作为 UI 组件库使用。 -->
    <style name="Theme.TvFoxBrowser" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/bg_dark</item>
        <item name="android:colorBackground">@color/bg_dark</item>
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryDark">@color/bg_dark</item>
        <item name="colorAccent">@color/brand_primary</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:fontFamily">sans-serif</item>
        <!-- 关闭系统默认焦点高亮(使用自定义焦点样式 bg_card / bg_icon_button) -->
        <item name="android:defaultFocusHighlightEnabled" tools:targetApi="o">false</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.TvFox</item>
    </style>

    <style name="PreferenceThemeOverlay.TvFox" parent="PreferenceThemeOverlay">
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <!-- 文本样式:用独立名避免被解析成父样式 Text 的子样式 -->
    <style name="TextTitle">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextSubtitle">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="TextCaption">
        <item name="android:textColor">@color/text_hint</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- TV 按钮 -->
    <style name="WidgetTvFoxIconButton">
        <item name="android:background">@drawable/bg_icon_button</item>
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:focusableInTouchMode">true</item>
    </style>
</resources>
