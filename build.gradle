plugins {
    id 'com.android.application' version '8.1.4' apply false
    id 'org.jetbrains.kotlin.android' version '1.9.22' apply false
}

// 仓库声明已统一移至 settings.gradle 的 dependencyResolutionManagement
// (启用 FAIL_ON_PROJECT_REPOS 后,此处不能再声明 allprojects/repositories,否则构建报错)
